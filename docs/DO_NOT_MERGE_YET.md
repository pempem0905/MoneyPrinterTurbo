# CI validation branch

`our-video-engine` is intentionally kept separate from `main` while the API-only wrapper is being validated.

Do not merge this branch into `main` until the engine-specific CI checks and deployment smoke tests pass.
