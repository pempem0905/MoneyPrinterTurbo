# Batch 1 file index

- `engine.py` — API-only secure runtime wrapper.
- `.env.engine.example` — non-secret environment template.
- `docker-compose.engine.yml` — API-only Docker profile.
- `ENGINE.md` — operator and integration runbook.
- `test/test_engine_entrypoint.py` — runtime hardening tests.
- `.github/workflows/ci.yml` — includes `engine.py` in compile/lint checks.
- `.gitignore` — excludes `.env.engine`.
